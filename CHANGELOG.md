# 更新日志

## 0.2.2

### 修复
- **回退命令的回执不再出现在聊天里**：客户端为"超出 10 轮窗口就置灰"读取范围时会自动执行一次 `/rollback list`，而命令回执（`command/run` + `command/done`）会写进会话日志并显示——于是每打开一个会话底部就多出几行 rollback 命令。现在只隐藏**本客户端自己发起**的命令回执（按命令 id 匹配），你手打 `/rollback <轮次>` 的回执照常显示。
- **不再按文本隐藏任何行（事故修复）**：0.2.1 之后曾短暂使用"行文本同时含 rollback 与 list/preview 就隐藏"的规则，而助手回复正文里本就包含这些词，导致**对话消息被隐藏**、每次重绘又隐藏一行。内容匹配已彻底移除，并加入硬保险：**文本超过 500 字符的行永不隐藏**。

## 0.2.1

### 正确性修复
- **文件列表不再出现"本轮没动过"的文件**：边界重扫在轮次结束时复查被监视文件，一发现"文件不见了"就记成**当前这一轮**的变更——可那个文件往往在**很多轮之前**就没了。于是回退一条纯对话轮次时，列表里会冒出早已消失的文件，甚至把更早回退删掉的文件重新"找回"。现在的归属规则是：记到**最后一次确认该文件存在的那一轮之后**的那一轮；若那一轮已经是被回退过的死轮次，**直接不记录**（无法归属就不猜，绝不生成错误的恢复）。
- **已回退轮次不再被重扫监视**：监视清单改为只认"创建/修改"记录，并按日志里的回退标记丢弃已死轮次的记录——被更早回退正确删除的文件，不会再被"发现"一次并复活。
- **压缩检查点文案**：331 字符（约 83 token）→ **136 字符（约 34 token）**，省 59%；仍完整保留四件事（机器生成、此后的消息已移除、工作区文件同时被还原、继续且不要提及）。新增字符预算测试防止它再变长。

## 0.2.0

### 回退入口
- **每轮结束后都有回退按钮**：正常轮次在回复的动作条上（位置不变），**被中断的轮次**在轮次页脚——被中断的轮次没有收尾回复，动作条上不可能有按钮
- **运行中一律禁止回退**：任何一轮处于进行中都整体拒绝并给出可操作的提示，等它结束或暂停后再回退
- **移除模型工具**：模型调用回退必然发生在"某一轮进行中"，与上一条冲突；回退始终由人发起

### 文件回退
- **标签分四类**：`恢复`（文件还在、写回旧内容 · 绿）、`找回`（已被删除的文件放回来 · 蓝）、`删除`（撤销该区间新建的文件 · 红）、`跳过`（无法恢复）
- **边界重扫 + 最后已知内容**：每轮结束时复查文件工具碰过的路径，把被 shell **改写或删除**的变化记到该轮 → 「模型用 `write` 建、之后被 `pwsh` 删掉」的文件也能回退了
- **空目录清理**：回退删掉它创建的文件后，把**已清空且创建时间落在被回退时间段内**的祖先目录一并删除（更早创建的目录会保留）
- **盘根写入兜底**：Windows 上 `write`/`edit` 无法操作盘符根目录正下方的文件（文件系统层写前会 `mkdir` 父目录，而盘根 `E:\` 会 EPERM）；插件包装 `ctx.fs.writeText` / `editText` 让这两个工具可用，并保留原文件的字面匹配语义、行尾风格与权限位
- **跳过并继续**：某个文件恢复不了**不再中止整次回退**——能恢复的照做、对话照样截断、结果里列出跳过项，且保留其记录以便下次回退重试（文件锁释放后即可成功）

### 正确性修复
- **超出保留范围的回退目标改为拒绝并告知范围**（此前会静默按最旧的保留记录恢复，得到错误状态且无提示）
- **不再重放已被回退的轮次**：日志只追加，回退只是追加一条标记；重启后从日志重建会把早已回退的轮次带回来（表现为文件列表出现旧文件，甚至可能删掉你后来重建的文件）
- **预读捕获的占位空串不再被当作"文件已清空"**（此前可能让回退把文件写空）
- **空列表不再声称"无文件变更"**：插件看不见 shell 直接写出的文件，这种话它保证不了

### 界面
- **欢迎页**：把整段对话回退掉之后正常显示，不会残留在对话中间或被带到别的会话
- **回退弹窗**：逐条列出受影响文件及其动作，点击文件可在编辑器打开
- 回退按钮的注册带自检与故障上报，"按钮静默消失"不再可能无声发生

## 0.1.0
- 首个版本：按轮次检查点（10 轮滑动窗口）、文件恢复/删除、用 `user/message` 表层 `replace` 做原位截断、`/rollback` 命令与 Web 回退按钮